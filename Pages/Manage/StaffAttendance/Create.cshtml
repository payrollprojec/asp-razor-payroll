@page
@model PayrollAppRazorPages.Pages.Manage.StaffAttendance.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Attendance</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Attendance.ApplicationUserId" class="control-label"></label>
                <select asp-for="Attendance.ApplicationUserId" class="form-control" asp-items="ViewBag.ApplicationUserId"></select>
                <span class="text-success">hi</span>

            </div>
            <div class="form-group">
                <label asp-for="Attendance.PunchDate" class="control-label"></label>
                <input asp-for="Attendance.PunchDate" class="form-control" />
                <span asp-validation-for="Attendance.PunchDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Attendance.AttendanceStatusId" class="control-label"></label>
                <select asp-for="Attendance.AttendanceStatusId" class="form-control" asp-items="ViewBag.AttendanceStatusId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
